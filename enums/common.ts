export enum TOKEN {
  CMS = 'cmsToken',
  WEBSITE = 'websiteToken'
}

export enum PLATFORM {
  CMS = 'cms',
  WEBSITE = 'website'
}

export enum EAlignEnum {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}